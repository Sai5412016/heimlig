# Heimlig – Projektkontext (Handoff für Claude Code)

Diese Datei macht Claude Code überall (Desktop **und** mobil/Web) sofort arbeitsfähig.
Sie wird über `CLAUDE.md` automatisch in jede Session geladen.

## Über das Projekt
- **Heimlig** – Haushalts-App für Paare, WGs & Familien (Einkaufslisten, Aufgaben/Kalender, Budget, Rezepte, Gamification).
- Macher: **Andreas Schilling ("Andi")**, Firma **Gut Feeling Labs**. Sprache: Deutsch, Du-Form, locker. Zielgruppe 18+.
- Dunkles, mobile-first Design; Markengrün `#2D6A4F`.

## Tech-Stack
- **Expo SDK 54** + `expo-router`, React Native + **react-native-web**. ⚠️ Vor Code immer die versionierten Expo-Docs (v54) prüfen (siehe AGENTS.md).
- **Supabase** (Postgres + Auth + RLS + Edge Functions). Projekt-Ref: **`eabwlyihcmofkbqtbryz`**.
- **EAS Build** (Android AAB), Expo-Account **`fledderman`**, App-Package **`com.fledderman.heimlig`**.
- **Vercel**: Web-PWA, **auto-deploy bei jedem Push auf `main`** (kein manueller Schritt).
- **Sentry** (`@sentry/react-native`) für Crash-/Error-Reporting. Org `gut-feeling-labs`, Projekt `react-native`. DSN + Init in `lib/sentry.ts`, `ErrorBoundary` mit eigenem Fallback-Screen (`components/ErrorFallback.tsx`) in `app/_layout.tsx`. Kein `SENTRY_AUTH_TOKEN` hinterlegt (also noch kein Source-Map-Upload beim Build) — Stacktraces sind bis dahin nur auf minifizierten Code gemappt.
- Git: **`Sai5412016/heimlig`**, Branch **`main`**.

## Release-Flow (Android)
Heimlig ist **live im Play Store** (offizieller Production-Release, kein geschlossener Alpha-Test mehr). Es gibt eine EAS-GitHub-Integration, die bei Push auf `main` automatisch einen Build auslöst (unabhängig davon, ob `eas build` manuell/per CLI erfolgreich läuft).
1. `android.versionCode` in `app.json` um 1 erhöhen.
2. `eas build --platform android --profile production` (läuft in der Cloud; im Web/mobil `EXPO_TOKEN` als Env setzen, damit kein interaktiver Login nötig ist). Push auf `main` triggert den Build i.d.R. auch automatisch über die GitHub-Integration.
3. Fertige **AAB** in Play Console → **Production** → „Neuen Release erstellen" → Versionshinweise (de-DE) eintragen → hochladen → veröffentlichen. *(Upload macht Andi manuell – kein API-Zugang.)*
4. Nach Veröffentlichung: in Supabase `app_config.latest_version_code` auf den neuen versionCode setzen → löst das In-App-„Update verfügbar"-Popup für ältere Nutzer aus.
- **Web** braucht keinen Build – Push auf `main` reicht (Vercel).
- Stand zuletzt: **versionCode 54** gebaut & veröffentlicht; `app_config.latest_version_code` = **50** (noch nicht auf 54 hochgesetzt). versionName = `1.0.2`.
- Keine Tester-Ankündigungsmail mehr nötig (App ist live, keine Google-Group-Benachrichtigung mehr).

## Bekannte Play-Console-Warnungen ("Empfohlene Aktionen")
Zwei wiederkehrende Hinweise im Play-Console-Release-Dashboard, beide **kein Blocker**, keine weitere Aktion nötig:
- **„Nicht mehr unterstützte APIs für randlose Anzeige" (edge-to-edge)**: Ökosystem-weites, aktuell nicht app-seitig behebbares Problem (React Native Core / react-native-screens / Google Material Components nutzen intern noch die alten `Window.setStatusBarColor`/`setNavigationBarColor`-APIs). Kein eigener Code betroffen (geprüft: kein Treffer für diese APIs in unserem Code). Tracking: github.com/expo/expo#37459. Löst sich mit zukünftigen Expo/RN-Updates von selbst.
- **„Einschränkungen für Größenänderung/Ausrichtung entfernen" (Großbild-Support)**: `app.json` hat `"orientation": "default"` → erzeugt `android:screenOrientation="unspecified"` (nicht restriktiv). `app/_layout.tsx` sperrt Ausrichtung nur zur Laufzeit auf Handys (`Dimensions.get('screen')`, `smallestSide < 600`dp) via `expo-screen-orientation` — exakt das von Google offiziell empfohlene Pattern für Apps, die auf Handys Portrait behalten wollen (Android 16+ ignoriert `setRequestedOrientation` auf Großbildschirmen ohnehin automatisch). Kein `resizeableActivity`/`minAspectRatio`/`maxAspectRatio` irgendwo gesetzt (auch nicht in node_modules-Manifests). Die Warnung ist vermutlich ein generischer, nicht pro-Build neu bewerteter Hinweis der reinen Anwesenheit der Orientation-Lock-API im kompilierten Code – funktional ist die App bereits konform.

## Supabase / Datenbank
- Schema-Änderungen werden **direkt** angewandt (Supabase-MCP `apply_migration`/`execute_sql` oder SQL-Editor) – **nicht** als lokale Migrations-Dateien getrackt.
- Wichtige Tabellen: `households`, `members`, `shopping_lists`, `shopping_items`, `tasks`, `transactions`, `recipes`, `meal_plans`, `member_scores`, `item_catalog`, `app_config`.
- RLS nutzt `is_household_member(household_id)` (Admin-Check: `is_household_admin(household_id)`). SECURITY-DEFINER-RPCs: `create_household_for_user`, `join_household_by_code`, `bump_item_catalog` (prüft jetzt `is_household_member(p_household)`, EXECUTE nur noch für `authenticated`), `bump_member_score` (atomares Punkte-Upsert, ersetzt ein früheres read-then-write), `send_daily_digest` (EXECUTE nur noch für `postgres`/`service_role` — läuft ausschließlich über den `daily-digest-hourly` Cron-Job, kein Client-Aufruf vorgesehen).
- **Security-Audit-Fixes (Session Juli 2026)**: `members`-Tabelle hatte eine kritische Lücke — die `members_update`-Policy prüfte nur `user_id = auth.uid()`, ohne `household_id`/`role` vor Änderung zu schützen; jeder eingeloggte Nutzer hätte sich selbst in einen fremden Haushalt versetzen oder sich `role='admin'` setzen können. Jetzt per `BEFORE UPDATE`-Trigger (`trg_prevent_member_field_escalation`) blockiert — `household_id`/`role`/`user_id` sind nach dem Erstellen unveränderlich (werden nur von `create_household_for_user`/`join_household_by_code` gesetzt). Zusätzlich: `households_update` jetzt admin-only (`is_household_admin`), `member_locations`/`household_messages`/`push_debug` prüfen jetzt, dass `member_id` wirklich zur eigenen Mitgliedschaft im jeweiligen Haushalt gehört (verhindert Spoofing fremder Mitglieder-IDs).
- `tasks.recurrence_day` / Transaktions-Vorlagen-Anker (`transaction_date` der Template-Zeile): monatliche/jährliche Wiederholungen rechnen ab einem festen Anker-Tag statt von der zuletzt erzeugten Instanz zu verketten — verhindert, dass ein Kurzmonat-Clamp (31. → 28. im Februar) sich dauerhaft festsetzt.
- Edge Function `extract-recipe` (Rezept-Extraktion aus URL/Text/Bild via Claude). `ANTHROPIC_API_KEY` ist ein Supabase-Secret. Deploy via `npx supabase functions deploy extract-recipe --project-ref eabwlyihcmofkbqtbryz` (verify_jwt an lassen!).
- **Tages-Digest-Push**: `pg_cron` (stündlich) + `pg_net` rufen `send_daily_digest()` auf — reine Postgres-Funktion (kein Edge-Function-Umweg, kein Service-Role-Key nötig). Die Funktion selbst gated auf „aktuelle Uhrzeit in Europe/Berlin == 8 Uhr" (self-gating löst das DST-Problem, da der Cron in UTC läuft). Sendet nur an Haushalte mit `households.digest_enabled = true` und nur wenn an dem Tag offene Termine anstehen. Direkt an Expos Push-Endpunkt, gleiche Token-Quelle (`push_tokens`) wie die `notify-message`-Funktion.
- Edge Function `timetree-import` (siehe „TimeTree-Wechsel" unten) – nutzt TimeTrees inoffizielle API, kein Secret nötig (Nutzer gibt eigene TimeTree-Zugangsdaten pro Aufruf ein, wird nicht gespeichert).
- Tester-Haushalte stehen auf `plan_tier = 'premium'` (Freunde, zahlen nicht).

## Internationalisierung (i18n) — Rollout läuft
- 🕒 **Englisch als zweite Sprache** (Pilot, noch nicht gepusht, lokal committet): `i18next` + `react-i18next` + `expo-localization` (`~17.0.9`, SDK54-kompatible Version — `npx expo install` schlug wegen eines Netzwerkfehlers beim Kompatibilitäts-Check fehl, Version manuell aus `node_modules/expo/bundledNativeModules.json` übernommen). Sprache ist eine reine Geräte-Einstellung (`store/useStore.ts` `language`/`selectLanguage`, AsyncStorage `@heimlig/language`), nicht in der DB — gleiches Muster wie `darkMode`/`themeId`. Beim ersten Start automatische Erkennung über `Localization.getLocales()[0]?.languageCode` (Englisch nur wenn Gerät explizit auf Englisch steht, sonst Deutsch als Default), danach manueller Umschalter in Haushalt-Einstellungen (`🌐`-Zeile neben Dark Mode/Design) überschreibt das dauerhaft. Übersetzungsdateien in `lib/locales/{de,en}.ts`, gegen ein gemeinsames `AppTranslations`-Interface (`lib/locales/types.ts`) getippt, damit ein fehlender Key in einer Sprache ein TS-Fehler ist. **Bewusst schrittweise ausgerollt** (Nutzerentscheidung, nicht alles auf einmal): Pilot deckt nur die Tab-Leiste (`app/(tabs)/_layout.tsx`) + die Sprachwahl-Zeile selbst ab, der Rest der App (Einkauf, Aufgaben, Budget, Rezepte, restliche Haushalt-Einstellungen, alle Modals/Alerts) ist noch komplett Deutsch fest verdrahtet und folgt Screen für Screen.

## Wichtige Dateien
- `app/(tabs)/shopping.tsx` – Einkauf + Rezept-Import-Trigger + Artikel-Autocomplete (`lib/groceries.ts`, `item_catalog`).
- `app/(tabs)/tasks.tsx` – Kalender/Aufgaben, Gamification-Badge/Scoreboard, ICS-Import (`lib/ics.ts`), Aufgaben-Detail + Bearbeiten.
- `app/(tabs)/budget.tsx` – Budget, CSV-Export/Import (`lib/dataIO.ts`), wiederkehrende Transaktionen.
- `app/(tabs)/household.tsx` – Mitglieder, Einladung/Beitreten, Multi-Haushalt-Switcher, Einstellungen (Name, Passwort, Gamification, Haushalt verlassen).
- `app/(tabs)/recipes.tsx` + `components/RecipeImportModal.tsx` – Rezept-Menü (Favoriten, planen) + Import (Link/Text/Foto).
- `app/_layout.tsx` – Session-Laden, Deep-Link `heimlig://join/CODE`, Orientierung (Handy Portrait/Tablet frei), Update-Check (`lib/appUpdate.ts`).
- `store/useStore.ts` – zentraler Zustand (Zustand/`zustand`).
- `lib/gamification.ts` – Punkte + Titel (monatlich, aus `tasks.completed_at`).

## Konventionen
- Web-spezifische Layout-Fixes mit `Platform.OS === 'web'` (z.B. Modals oben verankern, damit Tastatur nichts verdeckt).
- Bekannter, **vorbestehender** tsc-Fehler in `store/useStore.ts` (completeTask `completed_at` null vs undefined) – harmlos, blockiert den Metro-Build nicht.
- Nach Code-Änderung: `npx tsc --noEmit` über die geänderten Dateien laufen lassen.

## Play-Store-Versionshinweise (statt Google-Group-Mail)
Seit dem Production-Release gibt's **keine Tester-Ankündigungsmail mehr**. Stattdessen kommen die „Was ist neu"-Texte direkt ins `<de-DE>`-Feld bei „Versionshinweise" in der Play Console beim Release erstellen. Kurz, locker, Du-Form, 1-2 Sätze mit Emoji reichen (kein Betreff/Anrede/Signatur nötig, das ist nur für die alte Tester-Mail).

## TimeTree-Wechsel (Onboarding für Umsteiger)
TimeTree hat **keine offizielle Export-/Sync-API** (offiziell bestätigt von TimeTree Support) — deshalb zwei Wege in `app/(tabs)/tasks.tsx`, beide mit derselben Duplikat-Erkennung (`isDuplicateTask`, matcht wiederkehrende Termine über Titel+Wiederholung, einmalige über Titel+Datum+Zeit):
- 🔄 **Quickstart** (`TimeTreeQuickstartModal`) – für alle sichtbar. Schnelles manuelles Nacherfassen der wichtigsten wiederkehrenden Termine, kein Login nötig.
- 🔗 **Direkter TimeTree-Import** (`components/TimeTreeWebViewModal.tsx`, nativ-only) – nur sichtbar wenn `households.timetree_import_enabled = true`. Lädt TimeTrees echte Login-Seite (`https://timetreeapp.com/signin`) in einer WebView. Nach dem Login wird per `injectJavaScript` **innerhalb der TimeTree-Seite** gegen TimeTrees inoffizielle API gefetcht (`/calendars`, `/calendar/{id}/events/sync`) — der Browser hängt die Session-Cookie automatisch an (auch HttpOnly), Heimlig sieht nie Passwort noch Cookie, nur die fertigen Termin-Daten zurück per `postMessage`. Mapping in `lib/timetreeEvents.ts`.
  - ⚠️ **Google-Login geht in der WebView NICHT** – Google blockt OAuth-Login aktiv in eingebetteten WebViews („Dieser Browser ist evtl. nicht sicher"), erkannt am User-Agent/JS-Fingerprint. Betrifft nur den Google-Button, nicht TimeTrees normalen E-Mail+Passwort-Login. Kein sauberer Workaround möglich (Custom-Tabs-Cookie-Speicher ist auf Android getrennt von dem der App-WebView, ein Session-Cookie aus einem System-Browser käme also nicht in unsere WebView). Hinweistext in der Modal-UI erklärt das + verweist auf „Passwort vergessen" bei TimeTree als Workaround für Google-only-Konten.
  - **Bewusst nur für ausgewählte Haushalte freigeschaltet** (aktuell: „Birkensteig" + „Zuhause" [Tobi+Anja, Invite-Code `BB8836B2`] — „Dörschis" hatte den Flag testweise auch gesetzt, aber nur 1 Mitglied beigetreten, die eigentlich genutzte Gruppe ist „Zuhause"), weil das inoffizielle TimeTree-Endpunkte nutzt (ToS-Risiko, kann jederzeit brechen). Freischalten: `update households set timetree_import_enabled = true where id = '...'`.
  - Icon-Leiste im Aufgaben-Header (🎁/📥/🔄/📷/🔗) sitzt in einer horizontal scrollbaren `ScrollView` (`toolbarLeftScroll` in `app/(tabs)/tasks.tsx`) — läuft nie über, egal wie viele Icons je nach Haushalt-Flags aktiv sind.
  - API-Endpunkte identifiziert über [eoleedi/TimeTree-Exporter](https://github.com/eoleedi/TimeTree-Exporter) (Python-Referenzimplementierung). Frühere Variante nutzte eine Edge Function mit direkter E-Mail/Passwort-Abfrage — durch den WebView-Ansatz abgelöst (unterstützt Google-Login, kein Passwort-Handling mehr nötig).

## Bereits umgesetzt (nicht mehr offen)
- ✅ **Mehrere Einkaufslisten pro Geschäft** (DM/Rossmann/Aldi etc.) + umschalten/erstellen/löschen – volle UI in `shopping.tsx` (`ListPickerModal`, „Listen ▾"-Button, „Schnell erstellen"-Chips pro Supermarkt) + Store-Funktionen `switchList`/`createShoppingList`/`deleteShoppingList`.
- ✅ **Kalender farbiger** – Kategorie-Farben in Aufgabenliste & Monatsansicht (seit versionCode 45).
- ✅ **12 Akzent-Designs voll illustriert** – eigene Maskottchen + Empty-State-Illustrationen für Einkauf & Aufgaben (seit versionCode 49).
- ✅ **13. Theme „Matrix"** (seit versionCode 58): schwarz/grünes Terminal-Theme, technisch anders als die anderen 12 – die überschreiben nur `brand`/`brandLight`/`brandDark`/`accent` und folgen sonst dem Hell/Dunkel-Toggle (`hooks/useTheme.ts`), Matrix hat `forceDark: true` + überschreibt den kompletten Oberflächen-Stack (`background`/`surface`/`text`/…) in `constants/theme.ts` (`AppTheme`-Interface entsprechend erweitert), unabhängig vom Toggle. `background: 'transparent'` ist Absicht: `components/MatrixRain.tsx` (reines RN-`Animated`, keine Canvas/Skia-Dependency – jede Spalte ein `Animated.View` mit `useNativeDriver: true`, läuft komplett auf dem UI-Thread) malt Schwarz + animierten Glyphen-Regen einmal hinter dem Tab-Navigator (`app/(tabs)/_layout.tsx`, `sceneStyle`), jeder Screen lässt es durchscheinen statt selbst zu übermalen. Noch keine eigenen Maskottchen-Illustrationen (fällt auf Emoji-Empty-State zurück, bewusst so belassen). Respektiert `prefers-reduced-motion`/`AccessibilityInfo.isReduceMotionEnabled`.
- ✅ **„Rasen Gold" (pitch-gold) mit fliegendem Ball** (seit versionCode 59): `components/PitchBall.tsx` lässt alle 10-35s einen ⚽-Ball diagonal übers Display fliegen (RN `Animated`, `useNativeDriver: true`, Zufalls-Höhe/-Richtung/-Dauer), als Sibling *nach* `<Tabs>` in `app/(tabs)/_layout.tsx` gemountet (fliegt *über* dem Inhalt statt dahinter, siehe Vollbild-Hintergrund-Notiz unten). `ThemeMotif`s `pitch-gold`-Fall zeigt einen abstrakten Mid-Kick-Torwart-Star (Bein/Torso/Kopf aus Views) statt des alten Ball-Badges – bewusst *keine* 1:1-Nachbildung einer bestimmten Anime-Figur (Ideengeber war „Die Kickers"/Captain Tsubasa), sondern generisch gehalten wie alle anderen Theme-Motive (kein Logo, kein Ebenbild einer realen/urheberrechtlich geschützten Figur).
- ✅ **„Hexen Lila" (witch-purple) mit fliegender Hexe** (seit versionCode 59): `components/FlyingWitch.tsx`, gleiches Muster wie `PitchBall` (alle 12-40s ein Swoop quer übers Display, undulierender statt einfacher Bogen-Pfad, kleine Funken-Spur), als generische Hexe-auf-Besen-Silhouette aus Views (Hut/Robe/Besen) – bewusst *keine* Bibi-Blocksberg-Kopie (Ideengeber war ein Bibi-Screenshot), sondern abstrakt in den Theme-eigenen Lila/Dunkel-Farben gehalten, gleiche Grenze wie bei „Rasen Gold". `ThemeMotif`s `witch-purple`-Fall zeigt dieselbe Silhouette in klein statt nur einem Hexenhut.
- ✅ **„Rennsport Rot" (racing) mit fahrendem Boliden** (seit versionCode 59): `components/RacingCar.tsx`, gleiches Muster wie `PitchBall`/`FlyingWitch`, aber schnelleres Durchqueren (2,2-3,8s statt 4-10s, soll „Speed" vermitteln) + ein kurzes mitfahrendes Streckenstück (Asphalt-Tempolinien, blendet mit dem Auto ein/aus). Generischer roter Bolide aus Views (Karosserie/Kabine/Räder/Spoiler) – bewusst *kein* Ferrari-Logo/-Modell (Ideengeber war ein Ferrari-Foto), rot ist hier einfach die schon bestehende Theme-Markenfarbe. `ThemeMotif`s `racing`-Fall zeigt dieselbe Seitenansicht in klein statt der alten Draufsicht-Badge.
- ✅ **14. Theme „Task Force Orange" (tactical-ops)** (seit versionCode 59): neues Theme (Orange/Gunmetal, `constants/theme.ts`), inspiriert vom Vibe kompetitiver Taktik-Shooter (Ideengeber war ein CS2-Screenshot) – bewusst *kein* Spielname/Logo/Skin-Ebenbild, gleiche Grenze wie bei den anderen Themes. `components/BulletTracers.tsx` (gleiche Sparse-Crossing-Familie wie `PitchBall`/`FlyingWitch`/`RacingCar`) feuert alle 6-20s einen Burst aus 2-4 sehr schnellen Tracern (~400-600ms, `expo-linear-gradient` für den Fade-Trail), abwechselnd von links/rechts – simuliert „zwei Seiten im Schusswechsel", ohne zwei echte Figuren zu zeichnen. `ThemeMotif`s `tactical-ops`-Fall zeigt eine generische Operator-Silhouette (Helm/Visier/Weste). Noch keine eigenen Empty-State-Illustrationen.
- ✅ **„Comic Held" (comic-hero) mit fliegendem Helden + Blitz-Trail** (seit versionCode 59, Crash-Fix in 60): `components/FlyingHero.tsx`, gleiche Sparse-Crossing-Familie wie die anderen (2,6-4,2s Flugzeit, alle 10-35s), zwei Blitz-Zickzack-Formen (`Bolt`, aus rotierten Balken) begleiten den Helden als Speed-Effekt. Generische Silhouette (Umhang/Torso/Faust/rautenförmiges Emblem) – bewusst *kein* Superman-Ebenbild (Ideengeber war ein Superman-Fanart), kein „S"-Schild, kein Gesicht. `ThemeMotif`s `comic-hero`-Fall hat zusätzlich einen kleinen Umhang-Schwung hinter dem Rauten-Emblem. **Produktions-Crash behoben (versionCode 60)**: `Bolt` nutzte eine normale `View` statt `Animated.View`, bekam aber einen animierten `opacity`-Interpolationswert übergeben – die Bridge crashte hart beim Cast auf Double (`JSApplicationIllegalArgumentException`/`ClassCastException`), App ließ sich mit aktivem Comic-Held-Theme nicht mehr öffnen. Alle anderen Theme-Komponenten wurden danach systematisch auf dasselbe Muster geprüft (kein weiterer Fall gefunden).
- ✅ **„Rotlicht Arena" (red-light) mit Ampel-Puppe** (seit versionCode 59): `components/RedLightDoll.tsx` – anders als die Sparse-Crossing-Familie (Ball/Hexe/Auto/Tracer/Held) ein **dauerhaftes** HUD-Element oben rechts, das im Zufallstakt (rot 2,5-4,5s / grün 4-8s) zwischen „schaut her" (zwei Punkt-Augen sichtbar, roter Bildschirm-Blitz) und „schaut weg" (Augen ausgeblendet, grüner Blitz) wechselt. Komplett abstrakte Form (Kopf/zwei Dutts/Kleid aus Views, keine Zöpfe, kein echtes Gesicht) – bewusst *keine* Kopie der Squid-Game-Puppe/Wächter-Uniformen (Ideengeber waren Screenshots aus der Serie), Rot/Grün sind hier einfach die universellen Ampelfarben des Spielprinzips. Respektiert `prefers-reduced-motion` (dann statisch, „rot", ohne Blitz).
- ✅ **Volle Hintergrund-Übernahme für 6 weitere Themes** (seit versionCode 61, Crash-Fix in 62): pitch-gold, witch-purple, racing, tactical-ops, comic-hero und red-light haben jetzt (wie „Matrix") `forceDark: true` + `background: 'transparent'` in `constants/theme.ts`, plus eigene statische Szenerie-Komponente (`PitchField`/`NightSky`/`NightTrack`/`OpsBackdrop`/`CitySkyline`/`ArenaBackdrop`, alle in `components/`), die vor `<Tabs>` gemountet wird wie `MatrixRain`. Bewusst **kein** `Animated.View` in diesen 6 neuen Dateien (nur `LinearGradient` + statische `View`s) – reduziert nach dem Comic-Held-Crash bewusst die Angriffsfläche für dieselbe Fehlerklasse, die Bewegung übernehmen weiterhin die bestehenden Sprite-Komponenten (Ball/Hexe/Auto/Tracer/Held/Puppe), die unverändert *nach* `<Tabs>` gemountet bleiben. Die fliegenden Figuren/Charaktere sind weiterhin bewusst generisch (kein 1:1-Original-Ebenbild) – das war eine explizite Nutzeranfrage, wurde abgelehnt.
- **Produktions-Crash behoben (versionCode 62)**: `IndexOutOfBoundsException: getChildDrawingOrder() returned invalid index N (child count is M)`, fatal, Android — ein klassischer RN/Android-Bug, bei dem eine `ViewGroup` eine veraltete Kind-Anzahl für die Zeichenreihenfolge cached, wenn sich die Zahl ihrer direkten Kind-Views zur Laufzeit ändert. Ursache: `FlyingWitch`, `RedLightDoll` und `BulletTracers` gaben je ein bare Fragment (`<>...</>`) mit mehreren bzw. variabel vielen Wurzel-Elementen zurück (bei `BulletTracers` zusätzlich verschachtelt: variable `.map()`-Länge *und* `Tracer` selbst ein Fragment) – direkt als Geschwister von `<Tabs>` im selben Eltern-`View` in `app/(tabs)/_layout.tsx`. Fix: jede dieser Komponenten liefert jetzt genau eine feste Wurzel-`View` zurück (interne Variabilität bleibt eine Ebene tiefer isoliert), zusätzlich wurden die Backdrop- und Sprite-Slots in `_layout.tsx` in je eine dauerhafte Wrapper-`View` gepackt, damit die Kind-Anzahl der `Tabs`-Geschwister-Ebene nie schwankt.
- ✅ **Artikel einem Laden zuordnen** – `item_catalog.preferred_supermarket` merkt sich, wo ein Artikel meist gekauft wird; Hinweis-Chip „Meist bei X — dort hinzufügen" im Add-Item-Sheet routet den Artikel automatisch in die passende Laden-Liste (seit versionCode 50).
- ✅ **TimeTree-Feature-Parität** (Session Juli 2026, „Heimlig soll TimeTree überbieten"): WebView-TimeTree-Import (Google-Login), Feiertage im Kalender (`lib/holidays.ts`), Termine anpinnen (`tasks.pinned`), Countdown-Badge im Termin-Detail, Tages-Digest-Push (`send_daily_digest()` via pg_cron), Checklisten in Terminen (`task_checklist_items`), KI-Termin aus Foto (`extract-event` Edge Function), gemeinsame Stundenansicht (👥-View-Mode, `MemberDayView`). Alles in `app/(tabs)/tasks.tsx` + `app/(tabs)/household.tsx`.
- ✅ **Englisch als zweite Sprache** — vollständig ausgerollt (Session Juli 2026, Fortsetzung des Pilots): jede `.tsx`-Datei in `app/`/`components/` geht jetzt über `t()`. Bewusst weiter Deutsch belassen: Impressum/Datenschutz (rechtlich gebundene Texte, §5 TMG/DSGVO), Artikel-Katalog (`lib/groceries.ts`), Marken-/Supermarktnamen (`lib/brands.ts`), Feiertagsnamen (`lib/holidays.ts`), CSV-Export-Header (`lib/dataIO.ts`) — alles Daten, keine UI-Chrome. Reine `lib/*.ts`-Module ohne React-Baum (z.B. `lib/appUpdate.ts`, `lib/productScore.ts`) nutzen `i18n.t()` direkt statt des `useTranslation`-Hooks.
- ✅ **Haushalts-Währung** (Session Juli 2026, Folge des i18n-Rollouts — nicht jedes Land nutzt Euro): neue Spalte `households.currency` (text, Default `'EUR'`), Picker in den Haushalt-Einstellungen (💱-Zeile, gleiches Chip-Muster wie 🌐 Sprache) mit ~27 gängigen Währungen (`lib/currency.ts`). Bewusst **haushaltsweit statt pro Gerät** (anders als Sprache/Dark Mode) — Budget wird zwischen Mitgliedern gesplittet/summiert, gemischte Währungen in derselben Rechnung würden keinen Sinn ergeben. Admin-only (gleiches Muster wie `digest_enabled`/`gamification_enabled`). Formatierung manuell (`formatCurrency()`, Symbol + Dezimaltrennzeichen je nach App-Sprache) statt `Intl.NumberFormat`, da Hermes' ICU-Daten nicht zuverlässig volle CLDR-Währungsformatierung für jede Locale/Währungs-Kombination mitbringen. Bestehende Beträge in der DB sind reine Zahlen ohne Währungstag — bei nachträglichem Wechsel keine Umrechnung, nur neue Anzeige.

## Offene Roadmap / Tester-Wünsche
- 📅 **Google-Kalender-Sync (OAuth)** – großer Brocken, braucht Google-Cloud-Projekt/OAuth. *(ICS-Import ist als einfachere Alternative bereits umgesetzt.)*
